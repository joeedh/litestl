set(LIB
)

set(SRC
  PUBLIC export.h
  PUBLIC time.h
  PUBLIC cpu.h
  common.cc
)

if (NOT BUILD_WASM AND CMAKE_SYSTEM_NAME STREQUAL "Windows")
set(SRC 
    ${SRC}
    win32.cc
)
endif()

if (BUILD_WASM)
    set(SRC 
        ${SRC}
        wasm.cc
    )
endif()

lt_add_library(platform "${SRC}" "${LIB}" OBJECT)
